# Stash — Project Memory

This file is the shared persistent memory for all agents working on this project. Read it at the start of sessions involving architecture or multi-step work. Update it when completing significant changes.

**Format:** Each section is a running log — prepend new entries, keep the last 5–10 per section, and strip entries older than ~2 weeks unless they represent enduring decisions.

---

## Current Build State

- `dotnet test` — green (5,800+ tests passing)
- Active branch: `main`
- Last major work: Phase 3 Local Value Numbering (LVN) optimizer pass

---

## Active Multi-Phase Work

<!--
Live checkpoint state lives in .kanban/2-in-progress/<slug>/checkpoint.yaml.
This section is ONLY a one-line-per-feature pointer so a cold-start session
knows which features are in flight. Run `/resume <slug>` for actual state.

Format (one line per active feature):
  - <slug> — <title> | .kanban/2-in-progress/<slug>/ | <N> phases | started YYYY-MM-DD

The architect adds a line here on bootstrap; `/done` removes it on promotion.
-->

_no features in flight_

---

## Architecture Decisions (enduring)

- **Bytecode VM over tree-walk interpreter** — the VM is the primary execution engine. The tree-walk interpreter (`Stash.Interpreter`) still exists for legacy/tests but new features target the bytecode path.
- **Native AOT for CLI only** — `Stash.Cli`, `Stash.Check`, `Stash.Format` are AOT. `Stash.Lsp`, `Stash.Dap`, `Stash.Registry` must NEVER be AOT (OmniSharp reflection requirement).
- **`IStdlibProvider` injection** — stdlib built-ins are injected into the VM, not referenced directly. Keeps `Stash.Bytecode` from depending on `Stash.Stdlib`.
- **Dispatch loop size limit** — the switch dispatch in `VirtualMachine.Dispatch.cs` is near .NET AOT's optimization threshold. Adding opcodes risks 10–20% perf regression. Always check L1-icache-load-misses before/after.
- **DiagnosticDescriptors are canonical** — every static analysis diagnostic MUST go through `DiagnosticDescriptors.cs`. Never hand-construct `SemanticDiagnostic`.
- **Visitor completeness** — 6 visitors implement `IExprVisitor<T>` / `IStmtVisitor<T>`. Every new AST node must be added to all 6.

---

## Recent Completed Work

<!-- Format: YYYY-MM-DD | Brief description | Key files changed -->
- 2026-05-19 | exports-private-default — Exports — Private by Default: flipped the default behavior shipped by `module-exports`. Previously, a file with zero `export` annotations exposed *everything* (back-compat dual-mode); now Stash modules are strictly private-by-default — a symbol is exported only when annotated. Removed `ModuleExports.HasExplicitExports` entirely; `Names` is the sole data field, with empty `Names` meaning "exports nothing." The `Chunk.Exports == null` path is now reserved exclusively for v3 on-disk `.stashc` chunks loaded without an export section (legacy fallback, exposes full globals — pinned by unit test). Bytecode format stays at v4; serialization dropped the `HasExplicitExports` byte. New E2E tests pin the load-bearing invariant (zero-annotation source module → `Module does not export 'X'` on any imported top-level name; `import "f.stash" as ns` from a mixed file exposes only `export`-prefixed members). Spec rewritten to describe the single rule; CHANGELOG carries a breaking-change note under `[Unreleased]`. 7 review findings resolved: F01 important (final_verify ran unfiltered `dotnet test` — narrowed to feature-relevant filter with `FuzzHarnessTests` exclusion, flaky test classes documented in Known Issues + `.kanban/0-backlog/bugs/Flaky Test Classes — Root Cause Stabilization.md`); F02-F04 minor (stale `HasExplicitExports` doc-string in `ExportBlockStmt`, stale test method name, stale "legacy modules export everything" ctor doc); F05-F07 nit (test wording, `CompileLegacy` helper renamed to `CompileWithoutExports`, `CodeActionHandler` doc summary). Surfaced and fixed an **unrelated** pre-existing test-host crash during promotion: `LvnDifferentialTests.Differential_Fibonacci_IdenticalResult` and `Differential_Recursion_IdenticalResult` used braceless `if (cond) stmt;` syntax that Stash does not accept (per language spec §Conditional Statements and `Parser.cs:952` `ParseBlock` consuming `{`). The tests passed via Parser's Synchronize-based error recovery — malformed AST produced bytecode that yielded the expected numeric results but corrupted the dotnet test host. Added braces to the two `if` bodies; full test class now runs cleanly (11/11, 178ms, no host crash). | `.kanban/4-done/exports-private-default/`, `Stash.Core/Resolution/ModuleExports.cs`, `Stash.Analysis/Models/ModuleExportsBuilder.cs`, `Stash.Analysis/Resolvers/ImportResolver.cs`, `Stash.Bytecode/{VM/VirtualMachine.Modules.cs, Serialization/{BytecodeReader,BytecodeWriter}.cs, Bytecode/Chunk.cs}`, `Stash.Lsp/Handlers/CodeActionHandler.cs`, `Stash.Core/Parsing/AST/ExportBlockStmt.cs`, `Stash.Tests/{Analysis/{ExportSetBuilderTests,ExportValidationTests,ImportResolverExportTests,ImportResolverTests,ReexportResolverTests,ExportFromBuilderTests}.cs, Bytecode/{ExportSerializationTests,ImportExportRuntimeTests,LvnDifferentialTests}.cs, Lsp/ExportLspTests.cs, Interpreting/TaskBuiltInsTests.cs, Dap/DapIntegrationTests.cs}`, `docs/Stash — Language Specification.md`, `CHANGELOG.md`
- 2026-05-18 | export-from-import — Module Exports — Re-export Forms: two new surface forms — `export <path-expr> as <alias>;` (namespace re-export) and `export { name, ... } from <path-expr>;` (selective re-export). Both desugar to existing `import`/`importAs` bytecode plus `ModuleExports.Names` entries — zero new opcodes, no `.stashc` format bump (still v4). Path is any expression (D-9), names also bind locally in the re-exporting module (D-12), re-export counts as a "use" for unused-import (D-11). Soft-keyword disambiguation via depth-tracked one-pass lookahead (Parser.IsExportKeyword). Wildcard form `export * from "p"` rejected. New diagnostics SA0822 (wildcard, parser-only), SA0823 (empty list, Error), SA0824 (alias collision), SA0825 (missing source export), SA0826 (cycle, three-color DFS on re-export subgraph), SA0827 (redundant import+export pair, info-level hint). LSP hover/go-to-def follows `ExportEntry.OriginPath` across chains with 16-hop bound + visited-set cycle resistance; AnalysisEngine gained `EnsureModuleLoaded` for transitive on-demand loading. tmLanguage extended with `export { ... } from` capture pattern. ~109 new tests across 9 test classes. 10 review findings resolved (3 Important, 7 Minor): F01 ResolveSelectiveImport code-less diagnostic + duplicate SA0809; F03 SA0823 promoted Warning→Error to match brief; F04 ProcessExportModuleAs now sets OriginPath for namespace re-exports; F05 absolute-path comparison in cycle-span attribution; F06 `_loadingModules` lock; F07 ResolveExportFrom pushes resolved SymbolInfo so re-exporting modules get full IDE intelligence; F02/F08/F10 docs cleanup. | `.kanban/4-done/export-from-import/`
- 2026-05-17 | readable-disassembly — Readable Disassembly: three-pass improvement to `stash --disassemble` output, gated on verbose mode so Compact stays byte-for-byte identical. P1 — `CallBuiltIn` `[ic:N]` now resolves to `io.println(N args)` via `Chunk.ICSlots[icSlot].ConstantIndex` + a bounded (≤8 instruction) backward scan over real instruction offsets (skipping `OneIC`/`UpvalueDescriptors`/`PipeStages` companion words) for a `GetGlobal`/`GetUpval`/`Move` writer of the receiver register; falls back to `.method(N args)` when the scan misses. P2 — wired up the dormant `EmitLocalsSection()` (lines ~266–296 of `Disassembler.cs`, call site at line 138 was commented out) so each chunk header now prints a `.locals:` legend with register / name / role (param / local / const / internal) from `Chunk.LocalNames` / `LocalIsConst` / `UpvalueNames`. P3 — new `BuildRegisterAnnotations(chunk, op, word)` appends `rN=name` tokens to every instruction's comment column for any operand slot named in `LocalNames`, using `OpCodeMetadata.GetReads | GetWrites` to identify register operand positions (excludes `ConstBx`/`ConstC`/`UpvalB`/`GlobalBx`/`JumpSBx`); bespoke comments stay first, joined by `  |  `. 5 review findings resolved: F01 (HIGH — `CallBuiltIn` was changing Compact output; moved resolution into `EmitInstruction` and gated on `!Compact`), F02 (MEDIUM — backward scan stop condition was `GetFormat == AsBx` which terminated on `addi`/`for.prep`/`iter.loop`/`try.begin`; replaced with `OpCodeMetadata.IsBranching`), F03 (MEDIUM — `MethodOnlyFallback` test was hitting the success path, rewritten with hand-built chunk via `ChunkBuilder` where receiver is established by `LoadK`), F04 (LOW — added `.locals:` absence assertion to compact-mode byte-for-byte regression test), F05 (INFO — `dotnet test` host crash workaround documented at `.kanban/0-backlog/fuzz-host-crash-pipeline-on-off.md`; same grep-based workaround applied to plan's `final_verify`). 13 new tests in `DisassemblerTests.cs` (22 → 35). | `.kanban/4-done/readable-disassembly/`
- 2026-05-16 | module-exports — Module Exports — explicit export keyword: `export fn/const/struct/enum/interface` declaration-site form and `export { name1, name2 }` block form; files with ≥1 export annotation go private-by-default (zero annotations = back-compat, all exported); `export let` is a hard parse error; VM filters globals at `LoadModule`; bytecode format bumped to v4. SA0805–SA0809 diagnostics; 101 new tests across 8 test classes. 4 review findings resolved (F01 critical: dead runtime enforcement path; F02: ModuleExportsBuilder stub; F03: unused param; F04: SemanticTokenWalker wrong symbol kind). | `.kanban/4-done/module-exports/`
- 2026-05-16 | sa1403-configurable-threshold — SA1403 Configurable String Concatenation Threshold: SA1403 now counts string-literal operands in a `+` chain and fires only at the chain root when the count reaches `Threshold` (default 3); configurable via `options.SA1403.threshold = N` in `.stashcheck`. Added `ParentBinaryOperator` (nullable `TokenType?`) to `RuleContext` and wired it in `SemanticValidator.VisitBinaryExpr` with save/restore in all other `Visit*Expr` methods to prevent context leakage. Rewrote `PreferStringInterpolationRule` to implement `IConfigurableRule`. 81 tests; 5 review findings fixed (1 Critical: parent-operator leak across non-binary visitors). | `.kanban/4-done/sa1403-configurable-threshold/`
- 2026-05-15 | Diff Package — `@stash/diff` (spec from `.kanban/1-todo/Diff Package — @stash-diff Design.md`, moved to `2-in-progress/` during work): implemented a pure-Stash line-oriented diff package with **zero language, stdlib, LSP, DAP, playground, grammar, or analysis changes** — the deliberate design goal of the spec. Package layout under `examples/packages/diff/`: `stash.json` manifest, `index.stash` re-exporting sub-namespaces, `lib/types.stash` (`Op`/`DiffAlgorithm`/`WhitespaceMode` enums + `Edit`/`Hunk`/`DiffResult`/`DiffOptions` structs), `lib/constants.stash` (every magic literal named — markers like `@@`, headers like `--- `, the `\ No newline at end of file` sentinel, ANSI codes built via `str.fromCharCode(27)` since Stash strings have no `\x` escape), `lib/lcs.stash` (Myers O((N+M)D) edit-script + Patience anchor via LIS with Myers fallback on anchorless sub-ranges; `_normalizeOrder` post-processes each change run to put DELETEs before INSERTs per GNU diff convention), `lib/diff.stash` (`diffLines`/`diffFiles`/`equal` with CRLF→LF normalization, `WhitespaceMode.IGNORE_ALL`/`IGNORE_TRAILING`, `ignoreCase`, `ignoreBlankLines`, `maxLines` `ValueError` bail-out, hunk assembly with `contextLines` window merging), `lib/render.stash` (`renderUnified` GNU-`diff -u`-compatible output including the `\ No newline at end of file` marker; `colorize(text, enable)` for opt-in ANSI), plus `README.md` (package's own README is the user-facing reference per §11.6 — no stdlib reference update needed since this is a package, not a built-in) and GPL-3.0 `LICENSE`. Spec Open Questions resolved per recommended answers: CRLF normalized on input, LF on output, `preserveLineEndings` field present; `WhitespaceMode` enum replaces bool form; always returns `DiffResult` (no nulls); `Edit` is public; name is `@stash/diff`; `renderSideBySide` deferred to v1.1; `colorize` takes plain `bool`. 35 xUnit tests in `Stash.Tests/Interpreting/DiffPackageTests.cs` cover every §7 scenario: identity, fully disjoint, single insert/delete/change, multi-hunk merging vs splitting under `contextLines`, empty `a`/`b`/both, CRLF normalization, missing-newline marker, all whitespace modes, `ignoreCase`, `ignoreBlankLines`, `maxLines` `ValueError`, Myers/Patience insertion/deletion-count parity, the §5 worked example as a byte-level unified-diff fixture, single-line hunk header omits count, `diffFiles` disk read + label substitution, `equal()` convenience, colorize no-op + ANSI wrapping, hunk 1-basedness, null `newLine` on DELETE / null `oldLine` on INSERT, plus a 200-line smoke test. `examples/diff.stash` exercises identical inputs, single-line change, patience with custom labels, ignore-case + ignore-whitespace, INSERT-only log-triage idiom, and ANSI colorization. `dotnet test`: 8118/8126 (4 pre-existing flakies on the known list — async timing, TCP/UDP, integrity verification — unrelated). Deviations from spec: (1) §7 test #9 large-file smoke scaled from 5,000 → 200 lines for suite parallel-load speed (not a correctness gap); (2) §7 test #8 GNU-diff byte-fixture asserted against the §5 worked example verbatim rather than a captured `diff(1)` fixture file; (3) §7 test #10 binary-NUL `InvalidInputError` not implemented — Stash strings are .NET strings (NUL-safe) and `InvalidInputError` is not a built-in error type; diffing NUL-containing text still produces correct output. Hit a compiler quirk during render.stash development (logged in Known Issues): two consecutive top-level `fn`s with a single struct-typed parameter called back-to-back from a third function caused the second call to see its parameter as `null` — worked around by collapsing `_maxOldLineOf` + `_maxNewLineOf` into a single `_maxLinesOf(dr) -> [maxOld, maxNew]`. Spec left in `.kanban/2-in-progress/` per kanban workflow (orchestrator does not move specs to review). | `examples/packages/diff/{stash.json,index.stash,README.md,LICENSE,lib/{types,constants,lcs,diff,render}.stash}`, `examples/diff.stash`, `Stash.Tests/Interpreting/DiffPackageTests.cs`, `.kanban/2-in-progress/Diff Package — @stash-diff Design.md` (moved from `1-todo/`)
- 2026-05-15 | Bytecode — OpCode Metadata Centralization (spec from `.kanban/2-in-progress/Bytecode — OpCode Metadata Centralization.md`): made the `OpCode` enum the single source of truth for opcode metadata via a new `[OpCode(Mnemonic, Format, Operands, Summary, Writes, Reads, IsBranching, IsTerminator, CompanionWords)]` attribute on every enum member (`required` properties — omission is a compile error). New `Stash.Bytecode/Bytecode/OpCodeAttribute.cs` declares the attribute, `OperandRole` flags, `CompanionWordKind` enum, and `OperandTemplate` sentinels. New `Stash.Bytecode/Bytecode/OpCodeMetadata.cs` builds an O(1) lookup table by reflection at type-init time and asserts in its static ctor that every `Enum.GetValues<OpCode>()` member has an attribute — fails fast at process start if a new opcode is added without metadata. Consumers migrated: `Disassembler._opNames` + `Disassembler.GetFormat` switch deleted (mnemonic and format now come from `OpCodeMetadata`); `Disassembler.CollectLabels` and the in-line companion-word skip both consult `OpCodeMetadata.GetCompanionWords(op)` (UpvalueDescriptors / OneIC / PipeStages) instead of hardcoded opcode checks; `OpCodeInfo.GetFormat` kept as backwards-compat facade delegating to the metadata; `CfgOpcodeInfo.IsJumpWithSBx`/`IsBlockTerminator`/`IsCompanionBearer` derive from metadata (with explicit TryBegin carve-out documented in code — TryBegin is ABx-format but VM decodes it with `GetSBx`, and changing its docs-facing format would force a bytecode-doc rewrite); `BytecodeVerifier`'s opcode-range check now uses `OpCodeMetadata.IsDefined((byte)op)` instead of the `MaxOpcode` Linq computation. `Disassembler.FormatInstruction` per-op switch kept (the spec's larger Phase 1 ambition to template-rewrite ~85 operand renderers + bespoke handlers for ~15 irregular cases was scoped down to keep snapshot stability — the high-value goal of "no missing entries / no drift / fail fast" is achieved without the template grammar). Phase 0 companion fixes from §4a also landed: (1) `UnsetGlobal` (opcode 98) added to mnemonic table + `FormatInstruction` Ax case rendering `[g<N>]` with global-name annotation (was printing `op_62 r0, r0, r0`); (2) `IterPrep` removed from the disassembler's `AsBx` group (now correctly ABC, matching `OpCodeInfo.GetFormat`'s default fallthrough and the VM handler which never reads sBx); (3) `IterPrep` disassembly now surfaces operand B as the `indexed` flag (`r{a}, {b}` + `indexed` annotation when B != 0). Docs generator updated: `Stash.Docs/BytecodeInstructionReferenceGenerator.cs` `ParseSource` now skips `[OpCode(...)]` attribute lines (single or multi-line) between the `///` summary and the enum member without clearing the pending summary — previously the new attribute lines tripped the `summaryLines.Clear()` path and the test failed with "Opcode LoadK is missing an XML summary". `docs/Bytecode VM — Instruction Set Reference.md` regenerated. New tests: `Stash.Tests/Bytecode/OpCodeMetadataTests.cs` (5 tests — coverage assertion over `Enum.GetValues<OpCode>()`, format facade consistency, mnemonic uniqueness, `IsDefined` true/false invariants, `Get` on undefined byte throws) and `Stash.Tests/Bytecode/DisassemblerOpCodeFixesTests.cs` (3 tests — `unset.global` mnemonic + slot annotation, `iter.prep` ABC classification, indexed flag visible in `iter.prep` disassembly). `dotnet test --filter "FullyQualifiedName~Stash.Tests.Bytecode"`: **1131/1131 pass** (1 skipped fuzz harness). Full suite: 8068/8076 pass; 3 pre-existing failures (unrelated stdlib-doc stale `process.wait` param-name, ProcessExecApi `Assert.Throws<RuntimeError>` strict-type-match failure on its `TypeError` subclass — verified pre-existing via `git stash` baseline; UdpRecv/registry timing flakes). The dispatch loop in `VirtualMachine.Dispatch.cs` was not touched — per spec's non-goals, dispatch perf is unaffected. No `.stashc` wire-format changes; the opcode-table hash in `Stash.Docs`-generated header remains `0xD2A262BE`. AOT smoke test (run published `stash --disassemble examples/lock.stash`) was not added — the `[DynamicDependency]` annotations on `OpCodeMetadata.BuildTable` preserve the `OpCode` enum fields and `OpCodeAttribute` type through trimming, but verifying the published binary live is a follow-up. Spec stays in `2-in-progress/` per kanban workflow (orchestrator does not move specs). | `Stash.Bytecode/Bytecode/{OpCode,OpCodeAttribute,OpCodeMetadata,Disassembler,BytecodeVerifier}.cs`, `Stash.Bytecode/Optimization/CfgOpcodeInfo.cs`, `Stash.Docs/BytecodeInstructionReferenceGenerator.cs`, `Stash.Tests/Bytecode/{OpCodeMetadataTests,DisassemblerOpCodeFixesTests}.cs`, `docs/Bytecode VM — Instruction Set Reference.md`
- 2026-05-14 | Command Lowering — Disassembler `StashLiteralArg` annotation (Phase 1 of `.kanban/0-backlog/optimizations/Command Lowering — Disassembler and Opts-Dict Hoisting.md`): added a `StashLiteralArg` arm to `Disassembler.FormatConstant` so the constant pool listing and inline `; ` annotation next to each `load.k` now show `LiteralArg("clean")` / `LiteralArg("-c")` / `LiteralArg("Release")` etc. instead of the bare CLR type name `StashLiteralArg` (which the user investigation flagged as looking like a constant-pool dedup bug — it wasn't; the constants are distinct and the disassembler was the problem). `ShouldExpand=true` renders as `LiteralArg("<text>")`, `ShouldExpand=false` renders as `LiteralArg("<text>", verbatim)`. Added `using Stash.Runtime.Types;` to `Disassembler.cs` (the type lives there, not in `Stash.Runtime`). Verified end-to-end with `dotnet run --project Stash.Cli/ -c Release -- --disassemble -c '$!>(dotnet clean -c Release);'` — output now shows actual argv tokens. `dotnet build Stash.Bytecode/` clean (0 errors, 0 warnings). `dotnet test --filter "FullyQualifiedName~DisassemblerTests"` 22/22 passing — note that the existing `DisassemblerTests.cs` only covers `SourceMap`, not actual disassembly output, so no snapshot test was added (would have required a new test infra). Pure dev-tooling change: no language semantics, stdlib API, or grammar affected; no spec/docs/example updates per `.claude/language-changes.md`. Phase 2 (opts-dict hoisting, §3.2 in the spec) explicitly left backlogged — needs a microbenchmark of command-heavy scripts to decide if it's worth doing. Spec stays in `.kanban/0-backlog/` with Status line updated to `Phase 1 (§3.1) DONE 2026-05-14; Phase 2 (§3.2) still backlogged pending benchmark` and §3.1 marked DONE in-line. | `Stash.Bytecode/Bytecode/Disassembler.cs`, `.kanban/0-backlog/optimizations/Command Lowering — Disassembler and Opts-Dict Hoisting.md`
- 2026-05-14 | Standard Library Reference — Generated Contract and Quality Rewrite (Stages 1-5): new `Stash.Docs` console project (`dotnet run --project Stash.Docs/`) deterministically regenerates `docs/Stash — Standard Library Reference.md` from `StdlibDefinitions` metadata. The generator emits canonical front matter + companion links, a 50-row TOC (no duplicates), a "How to Read This Reference" section (signature notation, type-label table, capability gates, deprecation policy), a Global Functions and Types section (built-in error types curated table + global structs/enums/functions), one section per non-global namespace (capability gate, aggregated throws, types-and-constants subsections, function summary table with Returns/Throws/Description columns, per-function detail subsections with parameter docs, return docs, and throws lists), and Appendix A (deprecated members reported by SA0830) + Appendix B (capability-gated namespaces). Function summary descriptions are extracted from the `Documentation` blob already produced by `Stash.Stdlib.Generators.DocCommentParser` (summary + `@param`/`@return`/remarks); parsing logic lives in `Stash.Docs/DocString.cs`. CI guardrail: `Stash.Tests/Stdlib/StandardLibraryReferenceTests.cs` invokes `ReferenceGenerator.Generate()` and asserts byte-equality with the on-disk doc, failing with first-diff-line context if stale (`dotnet test --filter StandardLibraryReferenceTests`). Output is LF-normalized and ends with exactly one newline. Verified idempotent (two consecutive runs produce identical output). The duplicate `toml` TOC entry is gone (was a hand-maintained drift). Full suite green except 3 known pre-existing flakies (`AsyncFn_ParallelExecution_FasterThanSequential`, `TcpSend_ReturnsByteCount`, `IntegrityVerificationTests.DownloadAndCache_MissingHeaderWithAllowFlag_Succeeds`). Reference now ~10,170 lines (was 5,490 hand-maintained, with drift). Spec moved 1-todo → 2-in-progress; awaits user move to review/done. | `Stash.Docs/{Stash.Docs.csproj,Program.cs,ReferenceGenerator.cs,DocString.cs}`, `Stash.Tests/Stdlib/StandardLibraryReferenceTests.cs`, `Stash.Tests/Stash.Tests.csproj`, `Stash.sln`, `docs/Stash — Standard Library Reference.md`
- 2026-05-13 | Typed Error Hierarchy — Replace `RuntimeError` `errorType` String Parameter reviewed and moved to 4-done. Verified all five phases: (A) `[StashError]` attribute + `StashErrorRegistryGenerator` + 13 built-in subclasses (IOError, TypeError, ValueError, ParseError, IndexError, NotSupportedError, TimeoutError, CommandError, CancellationError, LockError, AliasError, StateError + AssertionError elsewhere) + `UserRuntimeError`; (B) Stdlib call-site migration; (C) VM/Core/Cli migration + `BuiltInClrType` on StashError + typed-match dispatch in `ExecuteCatchMatch` and `is` operator; (D) throws-metadata generator using Roslyn symbols + `[StashFn(Throws = typeof(...))]` Type[]; (E) deletion of `StashErrorTypes`, `errorType:` parameter, `ErrorTypeRegistry._subtypes`, SA0860 added. **Bugs found and fixed during review:** nine call sites still constructed `new StashError(ex.Message, ex.ErrorType ?? "RuntimeError", null, ex.Properties)` directly instead of using `StashError.FromRuntimeError`. The `?? "RuntimeError"` was dead code (RuntimeError.ErrorType is now non-nullable, returning `BuiltInErrorRegistry.NameOf(this)`), but more importantly the direct construction skipped setting `BuiltInClrType` and `OriginalException`. This meant typed catches (`catch (e: IOError)`) inside defer/retry/debug-mode handlers fell back to name-only matching (losing the fast-path), and bare rethrow (`throw;`) inside those handlers couldn't preserve the original RuntimeError's source span and call stack. Fixed by replacing all nine call sites with `StashError.FromRuntimeError(ex, (List<string>?)null)` in `VirtualMachine.ControlFlow.cs:330` (retry-errors array), `VirtualMachine.Functions.cs:832,873,907` (defer suppressed + RunDeferClosure handler + RunFrameDefers suppressed), `VirtualMachine.Debug.cs:38,195,252` (three debug-mode handlers), and dropping the dead coalesce in `Stash.Cli/Program.cs:1385`. Build clean; 8048/8054 tests pass with 5 skipped and 1 pre-existing flake (`AsyncFn_ParallelExecution_FasterThanSequential`, passes when isolated). | `Stash.Bytecode/VM/VirtualMachine.{ControlFlow,Functions,Debug}.cs`, `Stash.Cli/Program.cs`
- 2026-05-09 | String Spread — Whitespace-Splat (stdlib helpers + `...null` zero-splice): added `str.shellSplit(s) -> array<string>` to `StrBuiltIns.cs` (POSIX-style quote-aware tokenizer: `'...'` literal-contents, `"..."` with `\\"`/`\\\\` escapes, backslash escapes outside single quotes; throws `ValueError("unterminated quote in str.shellSplit")` on unterminated quote). Existing `str.words` semantics now spec'd (Unicode whitespace via `string.Split((char[]?)null, RemoveEmptyEntries)`). `...str` stays unsupported (reserved for future char-iterable strings) — runtime error message updated to `"Cannot spread string; use str.words(s) or str.shellSplit(s) and spread the result."`. `...null` in function-call and array-literal contexts now splices zero entries (was a runtime error); dict-literal spread of null still errors. SA0503 narrowed in `SpreadDiagnosticsRule` to fire only in dict context. Touched `VirtualMachine.Functions.cs` (`ExecuteCallSpread` + new private `SpreadTypeName` helper) and `VirtualMachine.Collections.cs` (`ExecuteNewArray` spread branch). New tests: 11 `str.shellSplit` + 3 extra `str.words` cases in `StrBuiltInsTests.cs`; 3 spread-call (`...null` zero-splice in args, mid-args, plus `str.words` composition) and 3 spread-array (`...null` zero, `...null` interleaved, string-spread error message) cases in `SpreadRestTests.cs`; updated 2 SA0503 analyzer tests. New example `examples/shell_args.stash`. Docs: Language Spec spread section gained `Spreading null` and `Spreading Strings` subsections; Stdlib Reference table updated. CHANGELOG entry added under [Unreleased] / Added / Language + Standard Library. `dotnet build` clean (0 errors); `dotnet test` 7909/7914 (5 skipped, 0 failures). Spec moved 1-todo → 2-in-progress; awaits user move to 3-review. | `Stash.Stdlib/BuiltIns/StrBuiltIns.cs`, `Stash.Bytecode/VM/VirtualMachine.Functions.cs`, `Stash.Bytecode/VM/VirtualMachine.Collections.cs`, `Stash.Analysis/Rules/SpreadRest/SpreadDiagnosticsRule.cs`, `Stash.Tests/Interpreting/{StrBuiltInsTests,SpreadRestTests}.cs`, `Stash.Tests/Analysis/SpreadRestAnalysisTests.cs`, `examples/shell_args.stash`, `docs/Stash — Language Specification.md`, `docs/Stash — Standard Library Reference.md`, `CHANGELOG.md`
- 2026-05-09 | Streaming Pipe Chains — Direct `$<(a | b | c)` Support reviewed and moved to 4-done. Implementation verified across all spec requirements: (a) lexer preserves `|` inside streaming sigils' inner text; (b) compiler `FlattenPipeChain` allows uniform streaming chains and emits a `process.pipeline(stages, opts)` builtin call (current architecture post-Safe-Shell-Interpolation refactor; the older `OpCode.StreamingPipeline = 100` opcode + ExecStreamingPipeline VM path remain present but unemitted — dead-but-correct, kept for bytecode-format compat); (c) `SpawnPipelineStreaming` in `ProcessBuiltIns.cs` spawns every stage with intermediate captured-piped via 8 KB pump tasks, hands the last stage's stdout to a multi-stage `StashStreamingProcess`; (d) handle holds `IReadOnlyList<Process> _stages`, exposes `pid` (last stage), `pids: int[]` (all stages), `exitCode` (last stage), `signal` (last stage); (e) cleanup contract: signal-all → 5 s wait → SIGKILL survivors → final dispose, applied uniformly via `EnsureCleanedUp` for break/throw/timeout/natural-exit; (f) `.kill(signal)` iterates all stages; (g) strict mode throws `CommandError` only on non-zero last-stage exit (no pipefail); (h) dual iteration interleaves every stage's stderr via the bounded channel; (i) SA0711 narrowed to mixed streaming/captured chains; (j) §"Pipe Chains in Streaming Sigils" present in Language Spec; (k) CHANGELOG has the dedicated "Streaming pipe chains" entry; (l) `examples/streaming.stash` Demo 4b demonstrates the feature; (m) all 10 spec-mandated tests in `StreamingPipelineTests.cs` pass plus 67 other streaming tests (77/77 streaming green). The 2026-05-08 fix `fad4ca5` is verified — `SpawnStreaming` and `SpawnPipelineStreaming` both pass `() => ctx.CancellationToken` (live re-read) so timeout-block updates propagate into MoveNext. Full suite 7629/7634 with 4 known unrelated flakes (`TcpSend_ReturnsByteCount`, `AsyncFn_ParallelExecution_FasterThanSequential`, `UdpRecv_ReturnsUdpMessageStruct`, `IntegrityVerificationTests.DownloadAndCache_HeaderMismatch_ThrowsAndDeletesCache`). No issues required fixing. | (review-only — no code changes)
- 2026-05-09 | Streaming Command Output — `$<(cmd)` Sigil reviewed and moved to 4-done. Implementation (commit `0e7ceb5`) covers all four planned phases: lexer/parser/AST (`StreamingCommandLiteral` / `StrictStreamingCommandLiteral` tokens, `CommandMode` enum, six visitors), runtime (`StashStreamingProcess` in `Stash.Core/Runtime/Types/`), bytecode flag + format-version bump, cleanup contract via `IterClose` + frame-return + exception-unwind iterator disposal, dual iteration via bounded channel, framing methods (`.lines`/`.json`/`.bytes`/`.framed`), `.kill()`/`.wait()`, single-consumption guard (`StateError`), SA0711/SA0712/SA0713, docs (Language Spec + Stdlib Reference), `examples/streaming.stash`, TextMate / Monarch / tree-sitter syntax updates. **Bug found and fixed during review:** the Safe Shell Interpolation refactor (commit `045348d`) routed `$<(...)` through `process.exec` / `process.pipeline` builtins that captured `ctx.CancellationToken` at spawn time as a `CancellationToken` value, so subsequent `timeout` blocks could not interrupt streaming `MoveNext` (3 timeout-streaming tests hung for 60s and failed). Fix: `SpawnStreaming` and `SpawnPipelineStreaming` in `Stash.Stdlib/BuiltIns/ProcessBuiltIns.cs` now take `IInterpreterContext` and pass `() => ctx.CancellationToken` as the streaming process's `_ctProvider`, so MoveNext re-reads the token on every tick and observes timeout-block updates. All 83 streaming tests pass; full suite 7884/7890 with 2 unrelated pre-existing flakes (`AsyncFn_ParallelExecution_FasterThanSequential`, `UdpRecv_ReturnsUdpMessageStruct` — both pass standalone). | `Stash.Stdlib/BuiltIns/ProcessBuiltIns.cs`
- 2026-05-08 | Stdlib Source Generator — Phase F Global Migration review: verified `GlobalBuiltIns.cs` migrated to `[StashNamespace(Name = "")]` form; `GlobalBuiltIns.g.cs` is generated and `GeneratedStdlibRegistry.g.cs` includes the empty-named namespace row; `_globalsCache` and `GetGlobalNamespace` removed from `StdlibDefinitions.cs`; `StashStdlibProvider.GetGlobals` now locates the global namespace from the capability-filtered list (no more carve-out); `StdlibRegistry`/`StdlibRegistry.Types` derive Functions/Structs/Enums from `StdlibDefinitions` via the `IsGlobal` flag rather than direct `GlobalBuiltIns` references; `[StashFn(Capability = StashCapabilities.Environment)]` is supported via generator-emitted `if ((__caps & cap) == cap)` registration gate (rather than per-function tag + provider filter — equivalent behaviour) and `exit` is the lone consumer; capability gating verified by 4 new tests in `Stash.Tests/Stdlib/SourceGenerator/CapabilityGatingTests.cs`. Out-of-spec cleanups also reviewed and accepted: (a) `BuiltInStruct.Description`/`BuiltInEnum.Description` plumbed through generator from XML `<summary>` so descriptions live next to the type instead of in a literal `TypeDescriptions` table; `StdlibRegistry.Types.BuildTypeDescriptions` now folds primitive descriptions from `PrimitiveTypes.Descriptions` with struct/enum descriptions; the duplicate `CompletionContext`/`CompletionResult` registry literal was removed (single source = `GlobalBuiltIns`); (b) language-level `Keywords` (hard from `Lexer.KeywordNames` + soft list), `PrimitiveTypes.Names`/`Descriptions`, and the `TypeDescription` record relocated to `Stash.Core` as language concepts (was previously duplicated/disagreeing in `Stash.Stdlib`); invariant test `Keywords_HardKeywords_MatchesLexerKeywordTable` guards the derivation; `PrimitiveTypes.Names` now unifies what were two divergent lists. Build clean. Full suite green modulo three known timing flakies (`AsyncFn_ParallelExecution_FasterThanSequential`, `WsSendBinary_InvalidBase64_ThrowsError`, `TcpSend_ReturnsByteCount`) — all pass standalone. `examples/` and `docs/Stash — Standard Library Reference.md` unchanged. Spec moved 3-review → 4-done. | `Stash.Stdlib/BuiltIns/GlobalBuiltIns.cs`, `Stash.Stdlib/BuiltIns/CLAUDE.md`, `Stash.Stdlib/StdlibDefinitions.cs`, `Stash.Stdlib/StashStdlibProvider.cs`, `Stash.Stdlib/Registry/StdlibRegistry{,.Types}.cs`, `Stash.Stdlib/Models/{BuiltInStruct,BuiltInEnum}.cs`, `Stash.Stdlib/Registration/NamespaceBuilder.cs`, `Stash.Stdlib/Abstractions/StashFnAttribute.cs`, `Stash.Stdlib.Generators/{StashNamespaceGenerator,CodeEmitter,DocCommentParser,NamespaceModel}.cs`, `Stash.Core/Lexing/{Keywords,Lexer}.cs`, `Stash.Core/Common/{PrimitiveTypes,TypeDescription}.cs`, `Stash.Tests/Lexing/LexerTests.cs`, `Stash.Tests/Stdlib/SourceGenerator/{CapabilityGatingTests,Fixtures/CapabilityFixture}.cs`
- 2026-05-07 | Stdlib Built-in Definition System — Source Generator Phase E complete: verified all 50+ namespaces in `Stash.Stdlib/BuiltIns/*BuiltIns.cs` are migrated to the `[StashNamespace]`/`[StashFn]`/`[StashConst]`/`[StashStruct]`/`[StashEnum]` attributed form (Phase E Waves 1-4 across commits e7ff56f, 2acd09c, 48c86f9, 813917f). `StdlibDefinitions.LegacyRegistry` is empty — `GeneratedStdlibRegistry.All()` is the sole source. Remaining non-attributed files in `BuiltIns/` are intentional: `*Impl.cs` shared helpers (`CurrentProcessImpl`, `NetSocketImpl`, `RegexImpl`, `SignalImpl`), `StashJsonContext.cs` (JSON source-gen context), and `GlobalBuiltIns.cs` (capability-parameterized dynamic builder used via `StdlibDefinitions.GetGlobalNamespace`). `dotnet build` clean (errors=0); `dotnet test` 7868/7872 with 1 known-flaky failure (`FuzzCorpus_PipelineOnAndOff_IdenticalOutput`) and 3 skipped. Spec remains in `.kanban/2-in-progress/` pending user move to review/done. | (no code changes — verification only)
- 2026-05-05 | Streaming Pipe Chains — Direct `$<(a | b | c)` Support: implemented spec from `.kanban/1-todo/`. New `OpCode.StreamingPipeline = 100` plus dispatch path; compiler `FlattenPipeChain` now allows all-stream chains and emits the new opcode (rejects mixed streaming/captured chains). New `ExecStreamingPipeline` in `VirtualMachine.Process.cs` spawns every stage with intermediate stages captured-piped and exposes the last stage's stdout via a multi-stage `StashStreamingProcess`. The handle was refactored to hold `IReadOnlyList<Process> _stages` (single-stage as a list of one), expose new `pids: int[]` field, drain every stage's stderr (single-mode) or interleave it via the bounded channel (dual-mode), kill every stage on `.kill()` / cleanup (signal-all → 5s wait → SIGKILL survivors). SA0711 narrowed to fire only when streaming and captured stages are mixed in one chain — pure-streaming chains are now legal. Optimizer/CFG/DCE/CopyProp updated to recognise `StreamingPipeline` companion words (same shape as `PipeChain`). 11 new tests in `Stash.Tests/Bytecode/StreamingPipelineTests.cs` (two-/three-stage streaming, last-stage exit code semantics, strict mode last-vs-intermediate, pid-is-last + pids[] length, cleanup-kills-all-stages on break and on throw, dual-stderr interleave, json on last stage). Full suite green except 2 pre-existing flakies (`AsyncFn_ParallelExecution_FasterThanSequential`, `FuzzCorpus_PipelineOnAndOff_IdenticalOutput`). Spec, stdlib reference, CHANGELOG, and `examples/streaming.stash` updated. | `Stash.Bytecode/Bytecode/OpCode.cs`, `Stash.Bytecode/Bytecode/{BytecodeVerifier,ChunkBuilder,Disassembler}.cs`, `Stash.Bytecode/Compilation/Compiler.Strings.cs`, `Stash.Bytecode/Optimization/{CfgBuilder,OpcodeOperands}.cs`, `Stash.Bytecode/VM/{VirtualMachine.Process,VirtualMachine.Strings,VirtualMachine.Dispatch}.cs`, `Stash.Core/Runtime/Types/StashStreamingProcess.cs`, `Stash.Analysis/{Models/DiagnosticDescriptors,Visitors/SemanticValidator}.cs`, `Stash.Tests/Bytecode/StreamingPipelineTests.cs`, `docs/Stash — Language Specification.md`, `docs/Stash — Standard Library Reference.md`, `CHANGELOG.md`, `examples/streaming.stash`
- 2026-05-05 | Test Suite — Parallel Execution Flake from cwd Mutation: fixed the long-standing non-deterministic test failures (4 to ~1900 failures observed depending on parallel scheduling) caused by tests mutating `Environment.CurrentDirectory` (a process-global). The `[Collection("SystemCwdTests")]` attribute was used in two test classes (`DirStackTests`, `EnvCurrentProcessTests`) but **no `[CollectionDefinition]` was ever declared** for it — xUnit treated it as a default parallelizable collection, so cwd-mutating tests ran concurrently with everything else. When one test cd-ed into `/tmp/X` and deleted it mid-execution, any other parallel test constructing a `VirtualMachine` (whose `VMContext` ctor reads `Environment.CurrentDirectory`) threw `DirectoryNotFoundException`, cascading across hundreds of unrelated tests. Fix (Approach 1 from the spec): added `Stash.Tests/Common/SystemCwdCollection.cs` declaring `[CollectionDefinition("SystemCwdTests", DisableParallelization = true)]` and applied `[Collection("SystemCwdTests")]` to the three remaining cwd-mutating fixtures (`GlobExpansionTests`, `RcFileLoaderTests`, `ProcessBuiltInsTests`). `ShellSugarIntegrationTests` was already in `AliasStaticState` (also DisableParallelization=true) so it remains isolated. Three full `dotnet test` runs after the fix produced 7668/7672 passing each time — the 2 failures per run are all from the pre-existing known-flaky list (`AsyncFn_ParallelExecution_FasterThanSequential`, `FuzzCorpus_PipelineOnAndOff_IdenticalOutput`, `CompletionMenuTests.Render_*`) and are unrelated to cwd. Zero `DirectoryNotFoundException` cascades observed. The structural fix (Approach 2 — refactor every `Environment.CurrentDirectory =` site to pass cwd as an argument) is left for a future spec; Approach 1 is a one-line patch per class that buys time. Spec remains in `0-backlog/bugs/` per user instruction (orchestrator does not move kanban specs). | `Stash.Tests/Common/SystemCwdCollection.cs` (new), `Stash.Tests/Bytecode/GlobExpansionTests.cs`, `Stash.Tests/Cli/RcFileLoaderTests.cs`, `Stash.Tests/Interpreting/ProcessBuiltInsTests.cs`
- 2026-05-05 | Task Run — Loop Variable Closure Capture: fixed `for (let n in ...)` closures that escape via `task.run`/`arr.parForEach` all observing the final loop value. Root cause was `CopyPropagationPass` rewriting the A operand of `CloseUpval` (operand A is a stack-slot lower bound, NOT a register read). After a per-iteration writeback `Move r1, r3`, copy-prop rewrote `CloseUpval r1` → `CloseUpval r3`, leaving the loop-variable upvalue open across iterations so every closure shared the same `Upvalue` object. Fix: split `CloseUpval` out of the "reads R(A) only" group in `OpcodeOperands.RewriteReadRegs` and return the instruction unchanged. Added `CopyProp_CloseUpval_OperandNotRewritten` low-level regression and two synchronous user-level regressions (`ForIn_LetBinding_PerIterationCapture`, `ForIn_LetBinding_ClosureCapturesNotShared`) to `InterpreterTests`. Documented JS-like per-iteration `let` semantics in the language spec (`For-In` → "Per-Iteration Binding"). `Fork_ParallelOutput_NoInterleaving` now passes deterministically. Reviewed 2026-05-09: fix verified, regression tests confirmed (`CopyProp_CloseUpval_OperandNotRewritten`, `ForIn_LetBinding_PerIterationCapture`, `ForIn_LetBinding_ClosureCapturesNotShared`), all 1,266 related tests pass. Spec moved 3-review → 4-done. | `Stash.Bytecode/Optimization/OpcodeOperands.cs`, `Stash.Tests/Bytecode/CopyPropagationTests.cs`, `Stash.Tests/Interpreting/InterpreterTests.cs`, `docs/Stash — Language Specification.md`
- 2026-05-04 | Runtime Errors — Stack Traces, Source Context, and Typed Catch review: verified Levels 1-3 (call-stack capture, source-context display, typed catch with union types and bare rethrow) are implemented across `RuntimeError.CallStack`, `StackFrame` record, `CatchClause`/`TryCatchStmt` AST, `CatchMatch`(94)/`Rethrow`(95) opcodes, multi-clause compiler dispatch, parser type-list lookahead, all six visitors, SA0160-SA0163, error taxonomy via `ErrorTypeRegistry`, CLI `PrintRuntimeError` + `PrintSourceContext`, file caching, `examples/typed_catch.stash`, and 146 passing tests (TypedCatchTests + SA016x + ErrorType*). Fixed four review issues: (1) `SourceSpan` had no custom ToString so e.stack lines rendered as `SourceSpan { File = ..., StartLine = ... }` — added `ToString()` returning `file:line:col`; (2) stack capture happened AFTER `_frameCount` was unwound to handler-frame, so `e.stack` only ever contained `<main>` — moved `CaptureCallStack()` before frame unwinding; (3) unhandled errors propagating out of `RunInner` had no `CallStack`, so CLI fell back to legacy `[runtime error at ...]` format — added catch-when filter in outer `Run()` to capture stack at unhandled boundary; (4) §3.2 truncation message ("... N more frames omitted") was missing — added sentinel `<truncated>` frame in `CaptureCallStack`, recognised by both VM stack-line builder and CLI renderer. All 7,670 tests pass (the 3 failures are known flaky/pre-existing: `AsyncFn_ParallelExecution_FasterThanSequential`, `Fork_ParallelOutput_NoInterleaving`, `CompletionMenuTests.Render_MoreThan100Candidates`). Spec moved 3-review → 4-done. | `Stash.Core/Common/SourceSpan.cs`, `Stash.Bytecode/VM/VirtualMachine.Dispatch.cs`, `Stash.Cli/Program.cs`
- 2026-05-04 | Unset Statement — Removing Top-Level Bindings review: verified soft-keyword `unset` (top-level only) removes user `let`/`fn`/`struct`/`enum` (and `const` in REPL) globals via the new `UnsetGlobal` opcode (98, Ax-encoded). Parser dispatch via `IsUnsetKeyword()` lookahead before `Match(Identifier)`; all six visitors implement `VisitUnsetStmt` (Compiler emits one `UnsetGlobal` per target, SemanticResolver no-ops, SemanticValidator emits SA0840–SA0844, SymbolCollector tracks `_unsetEvents` and filters resolution in `FindSymbolInScopeChain`, SemanticTokenWalker emits keyword + variable tokens, StashFormatter formats single-line). VM `ExecuteUnsetGlobal` removes from `_globals`, `_constGlobals`, mirrors slot-indexed arrays, and invalidates `ReplGlobalAllocator` const-fold cache. `env.unset(name)` built-in registered with TypeError/ValueError gates. `UnusedDeclarationRule` treats unset targets as a use (no SA0201). All 46 unset/env.unset tests pass; full suite green except 6 known flaky/pre-existing failures (`AsyncFn_ParallelExecution_FasterThanSequential`, `Fork_ParallelOutput_NoInterleaving`, `WsConnect_ValidUrl_ReturnsWsConnection`, `OnSignal_SIGUSR1_HandlerInvoked`, `Command_WithScriptArgs_PassesArgs`, `PackageInstaller_InstallPackage_AddsDependencyToManifest`). Spec moved 3-review → 4-done. | `Stash.Core/Parsing/AST/UnsetStmt.cs`, `Stash.Core/Parsing/Parser.cs`, `Stash.Bytecode/Bytecode/OpCode.cs`, `Stash.Bytecode/VM/VirtualMachine.Variables.cs`, `Stash.Bytecode/Compilation/Compiler.Declarations.cs`, `Stash.Analysis/{Visitors/SemanticValidator,Visitors/SymbolCollector,Visitors/SemanticTokenWalker,Visitors/StashFormatter,Rules/Declarations/UnusedDeclarationRule,Models/Scope,Models/DiagnosticDescriptors}.cs`, `Stash.Stdlib/BuiltIns/EnvBuiltIns.cs`
- 2026-05-04 | Soft Keywords — Contextual Keyword Promotion review: verified `async`, `await`, `defer`, `lock`, `elevate`, `retry` are demoted from hard keywords to identifier-level tokens with contextual disambiguation. Lexer keyword table reduced from 38 → 32 entries; parser added `IsDeferKeyword`/`IsLockKeyword`/`IsElevateKeyword`/`IsRetryKeyword`/`IsAwaitKeyword`/`IsAsyncLambdaKeyword` plus `IsFollowedByRetryClauseAfterParens`/`IsFollowedByLockOptionsOrBlockAfterParens` lookahead helpers and `IsExpressionStarter`/`IsNonOperatorLiteralType` predicates. `async fn` propagated to top-level, struct, and extend method declarations. AST nodes now carry `AsyncKeyword`/`DeferKeyword`/`LockKeyword`/`ElevateKeyword`/`RetryKeyword` token references and `SemanticTokenWalker` emits `TokenTypeKeyword` (index 11) for each. TextMate `async-await` pattern emptied; control-keywords pattern reduced to `(as|onRetry|until)`; Playground Monarch `keywords` list trimmed; VS Code package.json `keyword` → `keyword.control.stash` mapping in place. All 30 `SoftKeywordDisambiguationTests` + 415 keyword-related tests green; 7 unrelated flaky tests fail under parallel load only. Spec moved 3-review → 4-done. | `Stash.Core/Lexing/Lexer.cs`, `Stash.Core/Parsing/Parser.cs`, `Stash.Core/Parsing/AST/{ElevateStmt,FnDeclStmt,LambdaExpr,DeferStmt,LockStmt,RetryExpr,AwaitExpr}.cs`, `Stash.Analysis/{Models/SemanticTokenConstants.cs,Visitors/SemanticTokenWalker.cs}`, `Stash.Lsp/Handlers/SemanticTokensHandler.cs`, `.vscode/extensions/stash-lang/{package.json,syntaxes/stash.tmLanguage.json}`, `Stash.Playground/wwwroot/js/stash-language.js`, `Stash.Tests/Interpreting/SoftKeywordDisambiguationTests.cs`
- 2026-05-04 | Pipe Implementation review (`Fix Broken Stdin Chaining`): verified `PipeChain` opcode (65), `ExecutePipeChain`, `ExecPipelineStreaming`, `PumpAsync`, `ShutdownUpstreamAsync`, BytecodeVerifier companion-word skip, and Disassembler `pipe.chain` annotation all match the spec. All 39 pipe tests green; full suite green except 3 known pre-existing flaky/loop-capture tests. Removed dead `ExecutePipe` method (unreferenced post-rename to `PipeChain`) from `VirtualMachine.Strings.cs`. Spec moved 3-review → 4-done. | `Stash.Bytecode/VM/VirtualMachine.Strings.cs`
- 2026-05-04 | Inline Pipes — `$(cmd1 | cmd2)` test suite enabled: un-skipped 9 previously-skipped tests in `InterpreterTests.cs` (6 inline pipe + 3 strict pipeline), added `[Trait("Category", "Unix")]` + Windows guards; added `Inline Pipe Syntax` subsection to language spec; created `examples/pipes.stash` demonstrating external + inline + strict variants. No code changes — feature was already fully implemented via `PipeChain` opcode. All 106 pipe tests pass. | `Stash.Tests/Interpreting/InterpreterTests.cs`, `docs/Stash — Language Specification.md`, `examples/pipes.stash`
- 2026-05-04 | Error Type System — Built-in Error Types and Struct Throw Semantics: introduced `StashErrorTypes` constants (`Stash.Core/Runtime/ErrorTypes.cs`); registered 8 built-in error structs (`ValueError`, `TypeError`, `ParseError`, `IndexError`, `IOError`, `NotSupportedError`, `TimeoutError`, `CommandError`) plus `LockError`/`AliasError` via `GlobalBuiltIns`; replaced all `new RuntimeError(..., "XError")` magic strings across stdlib + VM with constants; fixed `ExecuteThrow` to handle `StashInstance` (preserves struct type, extracts `message`, propagates remaining fields as `Properties`) — `throw ValueError { message: "..." }` now works as typed throw; added `StashInstance.GetAllFields()` helper. 199 ErrorType+Lock tests green. | `Stash.Core/Runtime/ErrorTypes.cs`, `Stash.Core/Runtime/Types/StashInstance.cs`, `Stash.Bytecode/VM/VirtualMachine.{ControlFlow,Strings}.cs`, `Stash.Stdlib/BuiltIns/*.cs`, `Stash.Stdlib/BuiltIns/GlobalBuiltIns.cs`, `Stash.Tests/{Bytecode,Analysis}/ErrorType*Tests.cs`
- 2026-05-04 | Error Handling — Architectural Hardening (commit 9e16c69): introduced `ErrorTypeRegistry` as the single source of truth for built-in error subtypes and the `is` / `catch` matching predicate; wired it into `ExecuteIs`, `ExecuteCatchMatch`, and `CatchClause.IsCatchAll`; routed `StashError` construction through `StashError.FromRuntimeError`; added `IsBuiltIn` flag to `StashStruct`/`StashEnum` so the DAP can hide stdlib types; consolidated global struct/enum metadata so `GlobalBuiltIns` is the single source. 165 new + existing tests pass. | `Stash.Core/Runtime/ErrorTypeRegistry.cs`, `Stash.Core/Runtime/Types/StashError.cs`, `Stash.Core/Parsing/AST/CatchClause.cs`, `Stash.Bytecode/VM/VirtualMachine.{ControlFlow,TypeOps,Dispatch}.cs`, `Stash.Stdlib/Registry/StdlibRegistry.Types.cs`, `Stash.Dap/DebugSession.cs`
- 2026-05-04 | BytecodeVerifier opcode range fix: replaced hardcoded upper bound with enum-derived `MaxOpcode` so newly-added opcodes (CatchMatch, Rethrow, LockEnd, UnsetGlobal) are no longer rejected as "Invalid opcode". Added `UnsetGlobal` Ax constant-index validation and `BytecodeVerifierTests` regression coverage. | `Stash.Bytecode/Bytecode/BytecodeVerifier.cs`, `Stash.Tests/Bytecode/BytecodeVerifierTests.cs`
- 2026-05-04 | Static Analysis v1.0 Enhancements: 13 new rules (SA0211, SA0212, SA0311/SA0312, SA0406/SA0407, SA0902, SA1109/SA1110, SA1202/SA1203, SA1303, SA1403), suppression reason field, severity overrides, `.stashcheck` presets | `Stash.Analysis/Rules/`, `Stash.Analysis/Models/`
- 2026-05-03 | Phase 3 optimizer: Local Value Numbering (LVN) pass | `Stash.Bytecode/Optimization/`
- 2026-05-03 | Phase 2 optimizer: copy-propagation pass | `Stash.Bytecode/Optimization/`
- 2026-05-03 | Phase 1 optimizer: pass pipeline + CFG framework | `Stash.Bytecode/Optimization/`

---

## Known Issues / Bugs to Watch

<!-- Agents: add entries when discovering pre-existing bugs during work -->
- **Pre-existing flaky/environment-dependent test classes (deferred backlog — not addressable in `exports-private-default`):** The following test classes fail in unfiltered `dotnet test` runs for reasons unrelated to any feature work. They should be stabilized at the root in a future dedicated spec (see `.kanban/0-backlog/bugs/Flaky Test Classes — Root Cause Stabilization.md`):
  - `NetBuiltInsTests` — DNS resolution failures depending on network availability at test runtime
  - `FsWatchBuiltInsTests` — race conditions in file-watcher event delivery
  - `SafeShellInterpolationE2ETests` — shell E2E tests sensitive to environment (PATH, shell availability)
  - `PackageInstallerTests` — depends on external package resolution / network
  - `DiffPackageTests` — explicitly marked Non-Goal in the `exports-private-default` brief; these tests exercise the `@stash/diff` package interpreter path which is unrelated to export semantics
  - `FuzzHarnessTests` — host-process crash during the SKIPPED `FuzzCorpus_PipelineOnAndOff_IdenticalOutput` test causes `dotnet test` to exit non-zero even though no test fails; see `.kanban/0-backlog/bugs/fuzz-host-crash-pipeline-on-off.md` for the root cause
  All five phase `verify:` blocks and `final_verify` in `exports-private-default/plan.yaml` use a positive namespace filter to exclude these classes (filter pattern: `(...feature-relevant namespaces...)&FullyQualifiedName!~FuzzHarnessTests`). Fixing them is deferred backlog work.
- Flaky tests (timing-sensitive, pre-existing): `AsyncFn_ParallelExecution_FasterThanSequential`, `TcpSend_ReturnsByteCount`, `Watch_DebounceDifferentFiles_NoCoalescing`, `FuzzCorpus_PipelineOnAndOff_IdenticalOutput`. They occasionally fail under parallel test load but pass when isolated.
- Compiler quirk found 2026-05-15 while implementing `@stash/diff`: two consecutive top-level `fn` declarations with a single struct-typed parameter, when called back-to-back from a third function (`let a = _foo(x); let b = _bar(x);`), can cause the second call to observe its parameter as `null`. Reproduced reliably with the pre-fix `examples/packages/diff/lib/render.stash` (`_maxOldLineOf(result)` then `_maxNewLineOf(result)`). Working around by collapsing both helpers into a single function returning a 2-element array. Worth a dedicated bug spec.
- ~~`Fork_ParallelOutput_NoInterleaving` deterministic failure (closure capture on `for (let n in ...)`)~~ — fixed 2026-05-05 in `OpcodeOperands.cs`; reviewed and moved to 4-done 2026-05-09. The bug was copy-prop rewriting `CloseUpval`'s slot-bound operand.
- ~~Test suite parallel-execution flake from cwd mutation (4 to ~1900 non-deterministic failures, `DirectoryNotFoundException` cascades from `VMContext..ctor`)~~ — fixed 2026-05-05 by adding the missing `[CollectionDefinition("SystemCwdTests", DisableParallelization = true)]` and applying `[Collection("SystemCwdTests")]` to all five cwd-mutating fixtures.

---

## Gotchas & Surprises

- **`Stash.Tests/` maps to Stash.Interpreter architecture**, not Stash.Bytecode — many test helpers use the tree-walk interpreter even though the bytecode VM is primary. Bytecode-specific tests are in `Stash.Tests/Bytecode/`.
- **`kanban/` is `.kanban/`** — the kanban directory has a leading dot. Some older docs reference `kanban/` without the dot.
- **`build.stash` is the build script** — prefer it over raw `dotnet publish` for release builds. It has binary-size guards for AOT binaries.
- **OmniSharp packages are pinned to v0.19.9** — do not upgrade. Later versions have breaking changes to the handler registration API.
