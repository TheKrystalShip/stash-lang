# Fixture: bad_raw_subcommand_path

This file intentionally references a raw subcommand path to test the rawpath guard.

To run the next phase, execute: scripts/checkpoint/next-phase.stash with the slug.
