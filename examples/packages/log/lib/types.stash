/// The severity levels for log filtering.
enum LogLevel {
  DEBUG,
  INFO,
  WARN,
  ERROR,
  OFF
}

/// Represents a single log entry before formatting.
struct LogEntry {
  level,
  message,
  timestamp
}
