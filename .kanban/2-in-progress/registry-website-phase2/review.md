# registry-website-phase2 — Review (Pass 2)

> Produced by `/feature-review`. One finding per H2 section.
> Each finding header MUST be parseable: `## Fxx — [SEVERITY] short title`.
> `/resolve <feature> Fxx [Fyy...]` reads the selected section(s) and dispatches a Resolver.

**Scope reviewed:** commits `0d78f4a8..d1854674` on branch `feature/registry-website-phase2`
**Brief:** ../brief.md
**Generated:** 2026-06-04

**Headline read.** Re-review of the five pass-1 findings (CRITICAL F01, IMPORTANT F02, MINOR F03/F04/F05) after their fixes landed in `cbe7b774`, `932c0e03`, and `3669eee8`. **All five fixes are verified correct and no regressions were introduced.** The security-load-bearing F01 fix is genuinely airtight: `SafeUrl.AllowExternal` rejects every dangerous-scheme variant a package author can author (`javascript:`/`data:`/`vbscript:`/`file:`/protocol-relative `//evil`/leading-whitespace tricks/uppercase schemes), `PackageSidebar.cshtml` is the only sink that takes a package-authored URL, and a re-grep of `href="@…"`/`src="@…"` across `Stash.Registry.Web/` confirms `Repository` is the sole package-authored URL attribute in the whole project (the icon and JS are static `~/` paths, all other hrefs are server-constructed under `/packages/@…` or `/search?q=…`). F02 lands the 400 arm in all three page models in correct catch-clause order (above the 5xx and `Exception` arms), routes the message through a separate `ValidationError` property so the view renders a plain inline alert without the "Registry Unavailable" heading, and caps `PageSize` at the named `PagingLimits.MaxPageSize` (no magic 100); each model has a matching xUnit test that asserts 400 status + message presence + absence of `Registry Unavailable`/`502`. F03's chokepoint scanner now reads whole-file (multi-line `@Html.Raw(\n expr \n)` is detected) and gates safe-form-2 on an `ApprovedHtmlRawProperties = {ReadmeHtml}` allow-list combined with the existing `HtmlString` reflection check; teeth-tests cover production-`ReadmeHtml`-not-flagged, rogue-string-flagged, non-`HtmlString` property flagged, multi-line flagged, and non-approved `HtmlString` property flagged. F04's `Pages/Error.cshtml(.cs)` is minimal, contains no `@Html.Raw`, and binds `TraceIdentifier` through Razor encoding. F05's `[Theory]` escape tests cover all four nullable `Seg`-using methods with `+`/space/`%`/`/` inputs verifying `%2B`/`%20`/`%25`/`%2F` round-trips. The cross-cutting guards remain meaningful: only one production `@Html.Raw(Model.ReadmeHtml)` site in the project, zero Bucket-B vocabulary in rendered HTML, csproj declares only `Stash.Registry.Contracts`, and no new magic strings introduced. Baseline `dotnet test` reported 13462 passed / 0 failed / 6 skipped.

---

## 0 findings — fixes verified correct

The five pass-1 findings are all `fixed` and the fixes hold up to scrutiny. Specifically:

- **F01 (CRITICAL):** `SafeUrl.AllowExternal` correctly defends against `javascript:`/`data:`/`vbscript:`/`file:` (rejected because not in `{http, https, mailto}`), case variants like `JAVASCRIPT:`/`MAILTO:` (rejected via `StringComparer.OrdinalIgnoreCase`), protocol-relative `//evil.com` (rejected because `Uri.TryCreate(…, UriKind.Absolute)` returns false), and leading-whitespace tricks like `" javascript:"` / `"\tjavascript:"` / `"\njavascript:"` (`Uri.TryCreate` strips leading whitespace and the resulting scheme `"javascript"` is still not in the allow-list). Embedded NUL bytes cause `TryCreate` to fail → null. Package-authored `Repository` flows through `SafeUrl.AllowExternal` in `PackageSidebar.cshtml`; when disallowed, the URL is rendered as **plain Razor-encoded text** with no `href`, no new `@Html.Raw`. Integration tests in `PackagePageTests.cs:312-340` and `:342-367` lock in the behavior. Re-greps of `href="@`/`src="@` across `Stash.Registry.Web/` confirm `Repository` is the sole package-authored URL sink — every other `href` is server-constructed (`detailUrl`, `BuildPageUrl`, `PackageDetailUrl`, `altHref` which is forced to start with `/packages/@…` or `/search?q=…`), and the only two `src=` attributes are static `~/js/copy-install.js` and `/images/stash-icon.svg`. `PackageDetailResponse` carries no other URL fields, and `PackageSummaryResponse` cards already use only server-built `detailUrl` (already verified by the href sweep).
- **F02 (IMPORTANT):** `Search.cshtml.cs`, `Package.cshtml.cs`, and `Version.cshtml.cs` each include a `catch (RegistryClientException ex) when ((int)ex.StatusCode == StatusCodes.Status400BadRequest)` arm ordered **above** the 5xx and `Exception` arms, populating a distinct `ValidationError` property. The view branches render `ValidationError` as a plain `<div class="alert alert-error">` with no "Registry Unavailable" heading, no `@Html.Raw`, and no unencoded path. `Search.cshtml.cs:82` caps `PageSize` at `PagingLimits.MaxPageSize` (the named constant — no magic 100). Tests in `SearchPageTests.cs:201-227`, `PackagePageTests.cs:372-…`, and `VersionPageTests.cs:188-…` each assert `400 BadRequest`, message presence, and `DoesNotContain("Registry Unavailable")` / `DoesNotContain("502")`.
- **F03 (MINOR):** `ReadmeChokepointMetaTests.ScanFileForViolations` now reads via `File.ReadAllText` and matches with `RegexOptions.Singleline`, so multi-line `@Html.Raw(\n expr \n)` is detected (proven by `FailPathFixture_MultilineHtmlRaw_IsDetected_ByProductionScanner`). Safe-form-2 now requires the property name to be in `ApprovedHtmlRawProperties = {"ReadmeHtml"}` **and** typed as `HtmlString` — both conditions necessary, so a future `HtmlString FutureEvil` without an allow-list entry is flagged (proven by `FailPathFixture_NonApprovedHtmlStringProperty_IsFlagged_ByProductionScanner`). The production `@Html.Raw(Model.ReadmeHtml)` write is still recognized as safe (`ProductionPackageCshtml_ModelReadmeHtml_IsNotFlagged_ByProductionScanner`). The binding-floor still asserts ≥5 cshtml files scanned and ≥1 `@Html.Raw` found, defeating a vacuous pass. Pre-existing teeth tests (rogue string flagged, safe `RenderToSafeHtml(...)` not flagged, `Model.InstallCommand` string-typed property flagged via reflection) remain green.
- **F04 (MINOR):** `Pages/Error.cshtml(.cs)` exists, renders a generic "something went wrong" page with the `HttpContext.TraceIdentifier` correlated request ID (encoded by Razor — no `@Html.Raw`, no exception details leaked), and the `[ResponseCache(Duration = 0, NoStore = true)]` + `[IgnoreAntiforgeryToken]` decoration matches the standard ASP.NET error-page pattern. The `UseExceptionHandler("/Error")` registration in `Program.cs` now resolves to a real page.
- **F05 (MINOR):** Four `[Theory]` tests in `HttpRegistryClientTests.cs:426-...` cover `GetPackageAsync`, `GetVersionsAsync`, `GetReadmeAsync`, and `GetVersionAsync`, each with `InlineData` for the happy path plus the security-relevant cases (`scope+ / name space → %2B / %20`, `a%b / c/d → %25 / %2F`, and for `GetVersionAsync` the version `1.0+rc1 → 1.0%2Brc1`). A regression that drops the `Uri.EscapeDataString` call on any segment now fails the build.

No new findings surfaced during pass 2. The cross-cutting invariants — single `@Html.Raw` site, no `Stash.Registry` project reference, no Bucket-B label vocabulary in rendered HTML, no magic strings for bounded domains — all hold post-fix.
